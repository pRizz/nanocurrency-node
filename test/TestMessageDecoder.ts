import * as assert from 'assert'
import {MessageDecoder, MessageType, Stream} from '../src/node/Common'

// describe('Account', () => {
//     describe('#toNANOAddress()',() => {
//         it('should produce a nano address when called', async () => {
//             const messageStream = new Stream()
//             const messageDecoder = new MessageDecoder(messageStream)
//             const messageHeader = await messageDecoder.readMessageHeader()
//
//             assert.strictEqual(messageHeader.messageType, MessageType.keepalive)
//         });
//     });
// });
